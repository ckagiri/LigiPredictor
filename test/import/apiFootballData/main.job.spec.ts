describe('Main Job', () => {
  describe('start', () => {
    it('should get competitions from footballApiClient', () => {
      let mock = {};
      // let mainJob = new MainJob(client)
      // mainJob.start(queue)   
      // expect client spy called
    })
    it('should add CompetitionJobs to queue', () => {
      // mock client return 445
      // let epl = 445
      // let mainJob = new MainJob(client)
      // mainJob.start(queue)   
      // expect quue spy called with 445
    })
  })
})