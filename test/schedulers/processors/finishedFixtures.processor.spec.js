describe('Finished Fixtures', () => {
    describe('processPredictions', () => {
        it('should getPredictions for fixture that is FINISHED but not AllPredictionsProcessed', () => {
        });
        it('should not getPredictions for fixture that is not FINISHED', () => {
        });
        it('should not getPredictions for fixture that is FINISHED and AllPredictionsProcessed', () => {
        });
        it('should process PENDING predictions', () => {
        });
        it('should not process PROCESSED predictions', () => {
        });
        describe('getLeaderBoards', () => {
            it('should getSeasonboard', () => {
            });
            it('should have seasonBoard with UpdatingScores status', () => {
            });
            it('should getMonthboard', () => {
            });
            it('should have seasonBoard with UpdatingScores status', () => {
            });
            it('should getRoundboard', () => {
            });
            it('should have seasonBoard with UpdatingScores status', () => {
            });
            it('should cache boards', () => {
            });
        });
        it('should save userScores', () => {
        });
        describe('setToTrueAllPredictionsProcessed', () => {
        });
    });
});
//# sourceMappingURL=finishedFixtures.processor.spec.js.map